DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DB?sslmode=require

# Per-customer API keys. JSON: customer id -> array of valid secrets (supports rotation).
WEBHOOK_API_KEYS={"default":["replace_me_with_a_secret"]}
